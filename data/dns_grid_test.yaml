test:
  type: dns_grid
  period: 600
  health_settings:
    dns_valid_codes: [0]
  record_type: DNS_RECORD_AAAA
  servers: [1.1.1.1, 8.8.8.8]

targets:
  use:
  - www.example.com
  - api.kentik.com

agents:
  min_matches: 3
  match:
  - name: regex(.*-west-.*)
  - type: global
