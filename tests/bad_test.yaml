name: prod_grid
type: network_grid
period: 300
#ping:
#  timeout: 3000 # milliseconds
#  protocol: icmp
#trace:
#  timeout: 22500 # milliseconds
#  protocol: icmp
#  port: 0
#  limit: 30

targets:
#  - 1.1.1.1
#  - 8.8.8.8
  devices:
    - device_type: router
    - match_any:
        - label: edge router
#  interface_addresses:
#    family: ipv4
#    public_only: True
  sending_ips:
    family: any
    public_only: True
  snmp_ip:
    family: any
    public_only: True

agents:
  - agentImpl: IMPLEMENT_TYPE_RUST
  - family: IP_FAMILY_DUAL
  - name: regex(us-(east|west).*)